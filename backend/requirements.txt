# DocTransAgent — Backend Dependencies
# Install: pip install -r requirements.txt

# API Framework
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
pydantic-settings>=2.1.0

# Database
sqlalchemy>=2.0.25
chromadb>=0.4.22

# LLM / AI
openai>=1.0.0
httpx>=0.26.0

# Document Parsing
PyMuPDF>=1.26.0
pdfplumber>=0.11.0
PyPDF2>=3.0.0
python-docx>=1.1.0

# Optional: Language detection (falls back gracefully if missing)
langdetect>=1.0.9

# Optional: Local embedding fallback (only loads if model is already cached)
# sentence-transformers>=2.2.0
